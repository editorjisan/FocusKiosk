<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Storage & media permissions silently granted via DPM for adult media purging -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:name=".FocusKioskApp"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:preserveLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusKiosk"
        android:testOnly="false">

        <!--
            MAIN / SETUP WIZARD ACTIVITY
            Normal launcher activity that the user taps to configure policies.
            Once activated, this activity component is disabled to self-hide.
        -->
        <activity
            android:name=".ui.SetupWizardActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Optional HomeLauncherActivity kept as secondary without HOME category -->
        <activity
            android:name=".launcher.HomeLauncherActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />

        <!-- BLOCKED URL INTERCEPTOR (Deep-Link Blackhole) -->
        <activity
            android:name=".ui.BlockedUrlActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <!-- Social media deep links -->
                <data android:host="facebook.com" />
                <data android:host="*.facebook.com" />
                <data android:host="fb.com" />
                <data android:host="*.fb.com" />
                <data android:host="fb.watch" />
                <data android:host="*.fb.watch" />
                <data android:host="fb.me" />
                <data android:host="*.fb.me" />
                <data android:host="instagram.com" />
                <data android:host="*.instagram.com" />
                <data android:host="tiktok.com" />
                <data android:host="*.tiktok.com" />
                <!-- Adult domains -->
                <data android:host="pornhub.com" />
                <data android:host="*.pornhub.com" />
                <data android:host="xvideos.com" />
                <data android:host="*.xvideos.com" />
                <data android:host="xnxx.com" />
                <data android:host="*.xnxx.com" />
                <data android:host="xhamster.com" />
                <data android:host="*.xhamster.com" />
                <data android:host="redtube.com" />
                <data android:host="*.redtube.com" />
                <data android:host="youporn.com" />
                <data android:host="*.youporn.com" />
                <data android:host="onlyfans.com" />
                <data android:host="*.onlyfans.com" />
            </intent-filter>
        </activity>

        <!-- REAL-TIME ADULT MEDIA OBSERVER SERVICE -->
        <service
            android:name=".media.RealtimeMediaObserverService"
            android:exported="false" />

        <!--
            DEVICE ADMIN RECEIVER
            android:exported="true" is REQUIRED — the system must deliver
            broadcast intents to this component.
            android:permission restricts who can send intents to it.
        -->
        <receiver
            android:name=".admin.FocusDeviceAdminReceiver"
            android:exported="true"
            android:label="@string/app_name"
            android:description="@string/device_admin_description"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- BOOT RECEIVER: DirectBoot-aware restoration after device restart -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- SILENT UPDATE RESULT RECEIVER (PackageInstaller commit target & app replaced) -->
        <receiver
            android:name=".updater.UpdateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.focuskiosk.updater.INSTALL_RESULT" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- HARDWARE-BACKED RECURRING OTA CHECK ALARM RECEIVER (Internal, Non-Exported) -->
        <receiver
            android:name=".updater.OtaAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.focuskiosk.ACTION_CHECK_OTA" />
            </intent-filter>
        </receiver>

        <!-- FOCUS COUNTDOWN FOREGROUND SERVICE -->
        <service
            android:name=".service.FocusCountdownService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Countdown timer for kiosk lock" />
        </service>

        <!-- FAIL-SAFE RESTORATION RECEIVER (Exact AlarmManager trigger, Internal, Non-Exported) -->
        <receiver
            android:name=".policy.FailSafeRestoreReceiver"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.focuskiosk.ACTION_RESTORE_FAILSAFE" />
                <action android:name="com.focuskiosk.policy.ACTION_TIMER_EXPIRED" />
            </intent-filter>
        </receiver>

        <!-- WATCHDOG RECEIVER (Immediate restore on screen wake / boot) -->
        <receiver
            android:name=".policy.WatchdogReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.SCREEN_ON" />
            </intent-filter>
        </receiver>

    </application>
</manifest>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- Media purge worker permissions (granted silently via DPM as Device Owner) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:name=".FocusKioskApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusKiosk"
        android:testOnly="false">

        <!--
            MAIN / SETUP WIZARD ACTIVITY
            Normal launcher activity that the user taps to configure policies.
            Once activated, this activity component is disabled to self-hide.
        -->
        <activity
            android:name=".ui.SetupWizardActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Optional HomeLauncherActivity kept as secondary without HOME category -->
        <activity
            android:name=".launcher.HomeLauncherActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />

        <!--
            DEVICE ADMIN RECEIVER
            android:exported="true" is REQUIRED — the system must deliver
            broadcast intents to this component.
            android:permission restricts who can send intents to it.
        -->
        <receiver
            android:name=".admin.FocusDeviceAdminReceiver"
            android:exported="true"
            android:label="@string/app_name"
            android:description="@string/device_admin_description"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- VPN SERVICE -->
        <service
            android:name=".vpn.FocusVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- BOOT RECEIVER: re-enforces policies after device restart -->
        <receiver
            android:name=".admin.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- UPDATE INSTALL RESULT RECEIVER -->
        <receiver
            android:name=".updater.InstallResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.focuskiosk.updater.INSTALL_RESULT" />
            </intent-filter>
        </receiver>

        <!-- SILENT UPDATE RESULT RECEIVER (PackageInstaller commit target & app replaced) -->
        <receiver
            android:name=".updater.UpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.focuskiosk.updater.INSTALL_RESULT" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- FOCUS COUNTDOWN FOREGROUND SERVICE -->
        <service
            android:name=".service.FocusCountdownService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FAIL-SAFE RESTORATION RECEIVER (Exact AlarmManager trigger) -->
        <receiver
            android:name=".policy.FailSafeRestoreReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.focuskiosk.policy.ACTION_TIMER_EXPIRED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
